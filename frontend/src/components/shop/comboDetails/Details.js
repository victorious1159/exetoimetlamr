import React, { Fragment } from "react";
import ComboDetailsSection from "./ComboDetailsSection";

const Details = (props) => {
  return (
    <Fragment>
      <ComboDetailsSection />
    </Fragment>
  );
};

export default Details;
